# RotiWrap Website

Official bilingual landing page for RotiWrap Indonesia.

## Local preview

Open `index.html` in a browser.

## GitHub Pages

This repository is designed to deploy from the `main` branch and `/ (root)` folder.

## Custom domain

The included `CNAME` file points the site to:

`rotiwrap.id`
